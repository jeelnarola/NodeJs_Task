<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>single</title>
    <style>
        .box{
            display: flex;
            flex-wrap: nowrap;
            padding: 20px 100px;
            width: 100%;
        }
        .img-box{
            width: 400px;
            height: 500px;
            object-fit: contain;
            border: 1px solid black;
        }
        img{
            width: 100%;
            height: 100%;
        }
        .content{
            padding:0px 30px;
            width: 600px;
        }
    </style>
</head>
<body>
   <section>
    <div class="container">
        <div class="box">
            <div class="img-box">
                <img src="<%= data.image %>" alt="">
            </div>
            <div class="content">
                <h1><%= data.title %></h1>
                <h5><%= data.bloger_name %></h5>
                <h6>Category <%= data.category %></h6>
                <p><%= data.description %></p>
            </div>
       </div>
    </div>
   </section>
    <!-- <script>
        let id=document.URL.split("/")[4]
        console.log(id);
        const get=()=>{
            fetch(`http://localhost:8080/singlePage/${id}`)
            .then((res)=>{return res.JSON})
            .then((data)=>{
                console.log(data);
            })
        }
        get()
    </script> -->
</body>
</html>